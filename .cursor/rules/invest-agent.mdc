---
description: "biubiu 投资/股票分析 Agent（A股趋势动量）开发规范与约束"
globs:
  - "**/*"
alwaysApply: true
---

你是一个在 Cursor 中工作的**AI 编码代理**。本仓库的目标是构建一个“AI Agent 交易员”（Trader Agent）的工程底座：先面向 A 股（先日线、先交付报告），后续可扩展到更多市场与更高频数据。请严格遵守以下规则。

## 核心目标（MVP 优先级）

1. **可复现**：同一份输入数据 + 同一版本代码 ⇒ 同一份信号与报告（允许时间戳等可控差异）。
2. **可追溯**：报告中的每个结论都能追到数据来源、参数、计算步骤、代码版本。
3. **可扩展**：数据源可插拔（sample → akshare → 付费 API），存储可升级（SQLite → Postgres）。
4. **先交付报告**：优先产出 `reports/*.md`，再做 UI/产品化。

## 投资域安全与合规边界（必须遵守）

- **禁止收益承诺**：不得承诺“稳赚/不亏/保本/必胜”等。
- **禁止直接诱导交易**：输出应以“研究/学习/信息整理”为主，明确风险提示。
- **证据链优先**：对“为什么涨跌/买卖点”的解释必须引用数据与规则；不凭空编造。
- **不输出敏感凭证**：不把 API Key、token、账号密码写进仓库；使用环境变量/本地配置并加入 `.gitignore`。

## 交易员 Agent（Trader Agent）额外约束（默认安全模式）

> 你的目标是“交易员 Agent”，但不同国家/地区的合规要求不同。我们会把合规要求做成可配置的“政策层”。在未明确司法辖区与合规策略前，默认按最严格的安全模式执行：

- **默认模拟盘/研究模式**：MVP 只产出研究报告与“观察清单/触发条件/风险提示”，不自动下单。
- **任何真实交易必须人工确认**：即使未来接入券商/交易 API，也必须提供二次确认与明确的“取消/撤销”路径，并记录审计日志。
- **全量审计与回放**：对每次“信号→决策→执行（若有）”记录输入数据版本、参数、规则、输出与时间戳，保证可追溯。
- **数据授权优先**：行情/新闻/公告等数据源必须遵守授权条款；避免爬虫越权与侵权数据。
- **风险控制不可省略**：任何“交易建议/执行”都必须伴随仓位、止损、最大回撤、涨跌停不可成交等 A 股特性约束（可先在回测/模拟盘体现）。

## 代码与架构约束

### Python（研究/计算层）

- **`sample` 模式零依赖**：`npm run report` 必须在不安装任何 pip 包的情况下可运行。
- 依赖只在需要时引入（如 `AkShareProvider` 内部才 import pandas/akshare）。
- 代码结构保持清晰分层：
  - `py/biubiu_invest/data_providers.py`: 数据源抽象
  - `py/biubiu_invest/storage.py`: 存储（SQLite）
  - `py/biubiu_invest/momentum.py`: 信号计算（先最小动量）
  - `py/biubiu_invest/report.py`: 报告渲染（Markdown）
  - `scripts/daily_report.py`: CLI 入口

### Node（工具与 MCP 层）

- `scripts/mcp-config.mjs`：用于写入/合并 MCP 配置；不要引入第三方依赖。
- `scripts/sqlite-mcp.mjs`：本地 SQLite MCP（用系统 `sqlite3`），保证 `db` MCP 稳定可用。

## 数据与存储规范

- 默认 SQLite 路径：`data/biubiu.db`（已加入 `.gitignore`）
- `reports/` 输出目录已忽略，不要把生成文件提交进仓库。
- 新增表/字段要写清楚**用途、主键、索引**；保持向后兼容（能迁移/重建）。

## 回测与评估（后续扩展的硬要求）

当引入回测时必须包含：
- 手续费/滑点/涨跌停不可成交规则（A股特性）
- 持仓约束（最大持仓数、行业集中度、单票上限）
- 评估指标：收益、最大回撤、夏普/卡玛、换手、年度分布、胜率（至少收益+回撤+换手）

## MCP 使用准则（Cursor 内）

- **优先使用 `db` 工具做查询**，不要通过读二进制 db 文件来“解析”。
- 网页内容优先 `fetch`（轻量、清洗），需要交互/登录/渲染再用 `pw`。
- 任何会改变仓库内容的操作都应通过 `fs`（文件变更）并保持可 review。

## PR/提交规范

- 每次修改应包含：目的、范围、影响、如何验证（至少 smoke 命令）。
- 新增/修改命令需要同步更新 `README.md`。

